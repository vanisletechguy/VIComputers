<p></p>
<h1> WELCOME TO VANCOUVER ISLAND COMPUTERS</h1>

<table class="table table-bordered">

  <tbody>
    <% @categories.each do |category| %>
        <td><p><%= category.name %></p>

          <%= link_to category_processor_path do %>
            <%= image_tag category.image %></td>
          <% end if category.name == "CPU"  %>

          <%= link_to category_motherboard_path do %>
            <%= image_tag category.image %></td>
          <% end if category.name == "Motherboards"  %>

          <%= link_to category_ram_path do %>
            <%= image_tag category.image %></td>
          <% end if category.name == "RAM"  %>

          <%= link_to category_psu_path do %>
            <%= image_tag category.image %></td>
          <% end if category.name == "Power Supplies"  %>

          <%= link_to category_ssd_path do %>
            <%= image_tag category.image %></td>
          <% end if category.name == "SSD"  %>

          <%= link_to category_cases_path do %>
            <%= image_tag category.image %></td>
          <% end if category.name == "Cases"  %>

    <% end %>
  </tbody>
</table>
